services:
  - type: web
    name: fitness-ai-coach
    runtime: docker
    plan: standard
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: PORT
        value: "10000"
    dockerfilePath: ./Dockerfile
    dockerContext: .
