# TFLite - lightweight inference (~30MB vs TensorFlow 400MB)
# ai-edge-litert is Google's new TFLite package (works on all Python versions)
ai-edge-litert>=1.0.1
numpy==1.26.4
scikit-learn==1.5.0
joblib==1.4.2

# Pose estimation
mediapipe==0.10.14

# App
streamlit==1.35.0
python-dotenv==1.0.1

# Chatbot (pinned to avoid langgraph dependency explosion)
langchain==0.2.16
langchain-community==0.2.16
langchain-core==0.2.38
openai==1.37.0

# WebRTC for browser webcam (cloud deployment)
streamlit-webrtc==0.47.9
av==12.3.0

# Auto-refresh for live stats + voice during WebRTC streaming
streamlit-autorefresh==1.0.1

# Utilities
protobuf==4.25.3
pyyaml==6.0.1
requests==2.32.1
